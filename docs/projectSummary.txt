Cazzijobz Project Summary
📱 App Overview
Name: cazzijobz (Casual Jobs)
Mission: Community-driven platform connecting people offering casual labor (Workers) with those needing tasks done (Employers)
Target Audience: Lower-income communities worldwide for casual work (gardening, cleaning, construction, domestic work, etc.)
Location: South Africa focused, globally scalable
Monetization: Google AdMob (future implementation)

🛠 Tech Stack
Frontend: React Native + Expo

Backend: Supabase (Auth, Database, Storage)

State Management: React Context + AsyncStorage

Navigation: React Navigation

IDE: VS Code

Testing: Windows 10 + Android USB

🏗 Architecture Foundation
API Call Strategy
Type A (Immediate): Critical actions that need instant server sync

Signup, Login, Initial Profile Completion

Job posting, Job applications, Hiring decisions

Type B (Debounced): Non-critical updates that can be batched

Profile updates (name, bio, location, profile_pic)

Preference changes

Status updates

Timing Configuration:

Development: 1-minute timeout between Type B calls

Production: 5-minute timeout between Type B calls

Location: src/hooks/useProfileSync.js constants

AsyncStorage Strategy
Latest Data Wins: Subsequent changes overwrite previous pending changes

Automatic Retry: Failed syncs remain in storage for retry

Cross-session Persistence: Pending changes survive logout/login cycles

Storage Keys: userProfile, pending_profile_changes, last_api_call, isAdmin, authToken

File Structure
text
src/
├── navigation/
│   ├── AppNavigator.js          # Root navigator (Auth/Main stacks)
│   ├── AuthNavigator.js         # Authentication flow
│   ├── MainNavigator.js         # Main app tabs (Home, Profile, Admin)
│   └── NavigationConstants.js   # Route names
├── screens/
│   ├── auth/                    # Authentication flow
│   │   ├── WelcomeScreen.js     # Role selection
│   │   ├── TermsScreen.js       # Terms acceptance
│   │   ├── SignUpScreen.js      # User registration
│   │   └── LoginScreen.js       # User login
│   ├── shared/                  # Shared components
│   │   ├── profile/             # Profile management
│   │   │   ├── ProfileScreen.js # Main profile editor
│   │   │   └── components/      # Profile form fields
│   │   ├── CompleteProfileScreen.js # Initial profile setup
│   │   ├── HomeScreen.js        # Main dashboard
│   │   └── AdminScreen.js       # Admin panel
│   ├── employer/                # Employer-specific screens
│   └── worker/                  # Worker-specific screens
├── hooks/
│   ├── useSupabase.js           # API call management
│   └── useProfileSync.js        # Smart profile synchronization
├── utils/
│   ├── supabaseClient.js        # Supabase configuration
│   ├── supabaseService.js       # API service layer
│   ├── storageService.js        # AsyncStorage wrapper
│   └── debounce.js              # Debounce utility
└── styles/
    └── theme.js                 # Theming system (light/dark)

✅ Current Progress (Working Features)
1. Authentication Flow ✅
Welcome screen with role selection (Worker/Employer)

Terms & Conditions acceptance

Email/password signup with validation

Automatic profile creation in Supabase

Login with session persistence

2. Database Schema ✅
Profiles Table:

sql
id (UUID, primary key)
email (TEXT, required)
role (TEXT: 'worker'/'employer', required)
is_profile_complete (BOOLEAN, default false)
is_admin (BOOLEAN, default false)
full_name (TEXT)
bio (TEXT, 250 char limit)
location_city (TEXT)
location_suburb (TEXT)
created_at (TIMESTAMP)
updated_at (TIMESTAMP)
Storage Bucket: profile-pictures ✅

3. Navigation Structure ✅
text
Welcome → Role Selection → Terms → SignUp → CompleteProfile → Main (Home/Profile/Admin) (rudijonker@gmail.com = Admin user)
4. Theme System ✅
Comprehensive color palette (light/dark themes)

Consistent sizing system

Modular component styling

5. API Call Tracking System ✅
Location: src/utils/supabaseService.js

Tracks all Supabase calls with counters

Console logs with call numbers and types

Monitors API usage patterns

Key Functions:

authService.signUp() - Type A

authService.createProfile() - Type A

authService.updateProfile() - Type A (initial) / Type B (updates)

authService.getProfile() - Type A

🔄 Smart Profile Synchronization System
Core Implementation
File: src/screens/shared/profile/ProfileScreen.js

Immediate Local Save: User changes saved to AsyncStorage instantly

Timeout-based Sync: Type B calls debounced with configurable timeout

Pending Changes Management: Unsynced changes preserved across sessions

Smart Merge Logic: Local changes merged with server data on login

Key Behavior
User edits profile → Save to local storage immediately

Check timeout status:

Outside timeout (>1min): Make Type B API call immediately

Within timeout (<1min): Batch changes locally, sync later

Logout/Login: Pending changes preserved and merged

Error Handling: Failed syncs remain in queue for retry

Storage Service Functions
javascript
// Core functions in src/utils/storageService.js
setUserProfile(profile)           // Save user data locally
getUserProfile()                  // Retrieve user data
setPendingChanges(changes)        // Store unsynced changes  
getPendingChanges()               // Get pending changes
clearPendingChanges()             // Clear after successful sync
setLastApiCall(timestamp)         // Track API call timing
getLastApiCall()                  // Check timeout status
clearUserDataButKeepPendingChanges() // Logout without losing work
🎯 Next Priority Features
Immediate (Next Session):
Complete Profile Screen enhancements

Profile picture upload to Supabase Storage

Location detection with reverse geocoding

Role-based navigation flows

Short Term:
Worker dashboard (job browsing, applications)

Employer dashboard (job posting, applicant management)

Job management system

Long Term:
Chat System (post-hire communication)

Schedule Conflict Prevention

AdMob Integration


🔧 Technical Implementation Notes
Key Custom Hooks
useSupabase() - API call management and tracking

useProfileSync() - Smart profile synchronization (future enhancement)

Environment Setup
Supabase URL: https://wwuqejbsotizqdcwqfpu.supabase.co

Environment variables via transform-inline-environment-variables

Git branches: main (stable), profile-screen (current development)

Performance Considerations
Debounced API calls to minimize Supabase usage

Local storage caching for offline capability

Modular component structure for maintainability

Efficient re-rendering with proper state management

🚀 Why This Architecture Works
Scalable: Modular structure allows easy feature addition

Cost-effective: Smart API call strategy minimizes Supabase costs

User-friendly: Smooth onboarding with progressive profiling

Maintainable: Clear separation of concerns

Reliable: Robust error handling and data persistence

Offline-capable: Local storage ensures data availability

📋 Current Implementation Status
Working Features:
✅ User authentication and profile management

✅ Type A/B API call strategy with timeouts

✅ Local storage persistence across sessions

✅ Pending changes preservation during logout/login

✅ Admin role detection and navigation

✅ Theme system implementation

Files Recently Modified:
src/screens/shared/profile/ProfileScreen.js - Main profile editor with timeout logic

src/utils/storageService.js - Enhanced storage with pending changes support

src/screens/auth/LoginScreen.js - Smart login with change merging

src/navigation/MainNavigator.js - Admin detection and tab navigation

Current Focus:
Profile completion system with smart sync logic and API call optimization. The foundation is solid for building role-specific features.

🔄 API Call Patterns Established
Type A (Immediate) Examples:
User registration and login

Initial profile completion

Critical job actions (post, apply, hire)

Payment transactions

Type B (Debounced) Examples:
Profile field updates

Preference changes

Non-critical status updates

Search filter changes

This architecture should be extended throughout the app as new features are developed, maintaining consistent patterns for API calls, local storage, and user experience.

Last Updated: Current development on profile-screen branch
Current Focus: Profile completion system with smart sync logic
Next Session: Role-based navigation and dashboard implementation

*******************

💾 Update Project Summary
Let me add our recent progress to the project summary:

Add to docs/projectSummary.txt:

text
## 🎯 RECENT PROGRESS (Profile System Complete)

### ✅ Profile Screen Enhancements
- **Profile Picture Upload**: Integrated with Supabase Storage
- **Progress Completion System**: Visual progress bar (hides at 100%)
- **Simplified Image Picker**: Removed cropping for better UX
- **Type A/B API Strategy**: Image uploads (Type A) + profile updates (Type B)
- **Consistent UI**: Blue headings matching app theme

### 🛠️ Technical Implementation
- **Image Handling**: Local storage → Supabase Storage → Profile updates
- **Error Handling**: Silent failures with retry mechanism
- **Performance**: Debounced API calls prevent spamming
- **User Experience**: Seamless saving without confusing popups

### 📁 Key Files Modified
- `src/screens/shared/profile/ProfileScreen.js` - Enhanced with image upload
- `src/screens/shared/profile/components/ProfileImageField.js` - Image handling
- `src/utils/supabaseService.js` - Added uploadProfileImage function
- `src/utils/storageService.js` - Enhanced with pending changes support

### 🚀 Next Phase: Job Management System
**Priority Flow:**
1. Employer: Post Job → View Posted Jobs → Edit Jobs
2. Worker: Browse Jobs → Apply → View Applications
3. Employer: View Applicants → Hire/Decline
4. Worker: Notifications → Confirm Hire → Chat
5. Job Completion: Rating System → Auto-finalization

**Technical Considerations:**
- Job schedule conflict prevention
- Real-time notifications
- Chat system integration
- Rating and review system
🗺️ Logical Next Steps - Job Posting System
You're absolutely right - we need jobs before workers can apply. Let's start with:

Phase 1: Employer Job Posting
text
Create JobPostScreen → Job Management → Applicant Viewing
Database Schema Needed:
jobs table (title, description, location, wage, dates, etc.)

applications table (job_id, worker_id, status)

notifications table (user_id, type, message, read_status)

Key Features to Build:
Job Posting Form (reuse location components)

Job List Management (employer views their posts)

Application System (workers apply, employers manage)

🎯 Immediate Next Session:
When we continue, we should start with:

Create jobs table in Supabase

Build JobPostScreen for employers

Implement job creation with Type A/B calls

This follows the natural flow: Profiles → Job Creation → Job Browsing → Applications

The foundation is solid! Ready to build the core job marketplace functionality. 🚀

✅ All Systems Go:
✅ Profile images persist after logout/login

✅ Admin tab appears for admin users

✅ API calls working with proper Type A/B strategy

✅ Local storage functioning correctly

🗑️ Important Note: Account Deletion Cleanup
You're absolutely right! We need to handle storage cleanup when accounts are deleted. This is crucial for:

Data hygiene - Remove unused files

Cost optimization - Supabase storage costs money

User privacy - Complete data removal

🔧 Future Implementation - Account Deletion:
When we implement the "Delete Account" feature, we'll need:

1. Storage Cleanup Function in supabaseService.js:
javascript
deleteUserStorage: async (userId) => {
  try {
    // List all files in user's folder
    const { data: files, error } = await supabase.storage
      .from('profile-pictures')
      .list(userId);
    
    if (error) throw error;
    
    // Delete all user's files
    const filePaths = files.map(file => `${userId}/${file.name}`);
    const { error: deleteError } = await supabase.storage
      .from('profile-pictures')
      .remove(filePaths);
    
    if (deleteError) throw deleteError;
    
    return { success: true };
  } catch (error) {
    console.error('Storage cleanup error:', error);
    return { success: false, error };
  }
}
2. Complete Account Deletion Flow:
javascript
// 1. Delete from storage
await authService.deleteUserStorage(userId);

// 2. Delete from profiles table  
await supabase.from('profiles').delete().eq('id', userId);

// 3. Delete from auth (if possible via Supabase API)
// 4. Clear local storage
await storageService.clearAllData();
🚀 Ready for Next Phase:
With the profile system completely solid, we're ready to move to the core app functionality:

Next: Job Management System
Employers: Post jobs, manage applications

Workers: Browse jobs, apply, manage schedule

Both: Chat system, notifications, ratings

Database Tables Needed:
jobs - Job postings

applications - Job applications

notifications - User notifications

chats - Messaging system

ratings - User ratings

💾 Backup Current Progress:
bash
git add .
git commit -m "fix: Resolve admin detection and profile image persistence

- Fix admin status setting during login flow
- Restore profile_picture_url saving to database
- Verify all user flows working correctly
- Note storage cleanup requirement for account deletion"
git push origin profile-screen

****************************

📋 PROJECT SUMMARY FOR NEW CHAT
Cazzijobz Project Summary - Post Job System Complete ✅
🎯 Current Status
Post Job Screen Fully Implemented & Working

All form fields enhanced and optimized

Smart currency detection based on device locale

GPS location detection with reverse geocoding

Professional date/time pickers with auto-hour calculation

Compact, professional UI design

📱 App Overview
Name: cazzijobz (Casual Jobs)
Mission: Community-driven platform connecting Workers with Employers for casual labor
Target: Lower-income communities worldwide
Focus: South Africa, globally scalable
Tech: React Native + Expo + Supabase

🏗️ Architecture Foundation
API Strategy: Type A (Immediate) vs Type B (Debounced) calls
State Management: React Context + AsyncStorage
Navigation: React Navigation with Auth/Main stacks
Database: Supabase (PostgreSQL)

✅ COMPLETED FEATURES
1. Authentication System ✅
Role selection (Worker/Employer)

Email/password signup/login

Session persistence

Admin detection

2. Profile Management ✅
Complete profile system

Profile picture upload to Supabase Storage

Smart sync with Type A/B API strategy

Local storage persistence

3. Post Job Screen ✅ 🎯 CURRENTLY COMPLETE
Field Components:
JobCategoryField: Modal picker with icons (11 categories)

JobDescriptionField: Text area with character counter

JobLocationField: GPS detection + manual city/suburb inputs

JobDateTimeField: Native date picker + clock time pickers + auto-hour calculation

JobBudgetField: Currency detection + formatted input

Key Features:
Smart Validation: User-friendly error messages

Confirmation Modal: Job preview before posting

Auto-calculation: Hours calculated from time range

GPS Integration: Reverse geocoding for location

Currency Detection: Based on device locale settings

🗃️ Database Schema (Ready)
sql
-- Jobs table created in Supabase
-- Includes: employer_id, category, description, location fields,
-- scheduled_date, time_from, time_to, duration_hours, budget, status
🎨 UI/UX Optimizations
Compact field sizes (small fonts, tight spacing)

Professional styling matching app theme

Intuitive pickers (clock interface for time)

Auto-formatting (currency, hours calculation)

Helpful tooltips and information buttons

🚀 IMMEDIATE NEXT PRIORITIES
Phase 1: Job Management
My Jobs Screen - Employer views posted jobs

Job Cards - Display with status badges

Job Details Screen - View applicants + edit pre-hire

Job Editing - Reuse PostJobScreen for edits

Phase 2: Job Discovery & Applications
Worker Home Screen - Browse open jobs

Job Application System - Apply to jobs

Application Management - Track applications

Phase 3: Hiring & Execution
Hiring System - Employer hires/declines applicants

Chat System - Post-hire communication

Rating System - Job completion reviews

🔧 Technical Implementation Notes
File Structure
text
screens/jobs/post/
├── components/
│   ├── JobCategoryField.js    ✅
│   ├── JobDescriptionField.js ✅
│   ├── JobLocationField.js    ✅
│   ├── JobDateTimeField.js    ✅
│   └── JobBudgetField.js      ✅
├── hooks/
│   └── usePostJob.js          ✅
└── PostJobScreen.js           ✅
Key Dependencies
@react-native-community/datetimepicker - Date/time pickers

expo-location - GPS detection

expo-localization - Currency detection

@expo/vector-icons - UI icons

Navigation
Post Job screen integrated in bottom tab navigator

Working navigation flow: Form → Preview → Success

💾 Current Git Status
Branch: main (stable)
Last Commit: "feat: Complete Post Job screen with enhanced fields"
Ready For: My Jobs screen implementation

🎯 Success Metrics Achieved
✅ Form validation working

✅ GPS location detection accurate

✅ Currency auto-detection functional

✅ Time pickers user-friendly

✅ Auto-calculation working

✅ Professional UI/UX

✅ No syntax errors

🔍 Current Status Check
What's Working:
✅ Form UI and validation
✅ Field components and user experience
✅ Confirmation modal and flow
✅ Mock submission (alert success)

What's Missing:
❌ Actual Supabase API integration
❌ Type A/B API call strategy implementation
❌ AsyncStorage for job data persistence
❌ Error handling for real API calls
❌ Navigation to My Jobs after posting

****************************************

📝 Updated Project Summary Addition:
markdown
## 🔄 API Integration Status
**Post Job Screen**: 
- ✅ UI and validation complete
- ✅ Type A API call ready (immediate Supabase posting)
- ❌ AsyncStorage integration pending
- ❌ Offline queue system pending

**Next Priority**: 
1. Test Supabase job posting
2. Implement AsyncStorage for draft jobs
3. Build My Jobs screen for job management